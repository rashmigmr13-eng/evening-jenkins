pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo 'Building'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing in process'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploying in process'
            }
        }

    }
}

